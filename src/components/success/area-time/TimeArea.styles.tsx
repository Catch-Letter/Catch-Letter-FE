import { colors } from '#/styles/color'
import { css } from '@emotion/react'

export const TimeAreaStyle = css`
  display: flex;
  gap: 8px;
  font-weight: 700;
  margin: 0 45px 28px 57px;
  color: #fff;

  .time {
    color: ${colors.neonGreen[6]};
  }
`
